# TwitterImageDownloader2
browser extension for download image in twitter

# Build
```
> npm install
> npm run build
```

After successful build,  `twitterimagedownloader.xpi` file will show in release folder.